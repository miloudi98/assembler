section text {

fn _start {
    //mov(@b64 [rax] + disp + 1 + 2 + 3 - 4 - 5 + other_name, b64 disp);
    //// 64-bit memory offset.
    //mov(b64 rax, @b64 disp);
    //mov(@b64 [rax] + disp, b64 1 + 2 + 3);
    mov(@b64 [rax] + 0x1122334455, b64 disp);
    //// 64-bit memory offset.
    //mov(b64 rax, @b64 disp);
    //// 64-bit memory offset.
    //mov(b64 rax, @b64 disp);
    //mov(b64 rax, b64 100);
}
}
