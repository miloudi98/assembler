struct Node {
    val: i32,
    next: Option<Box<Node>>
}

impl Node {
    fn new() -> Self {
        Self {
            val: 0,
            next: None
        }
    }
}

fn main() {
    println!("Bismillah");

    let mut x = vec![]; let mut z = 0; let mut c: Vec<u32> = vec![]; let mut zz = x;

    let mut y = x;

    x[0] = 0;
}
